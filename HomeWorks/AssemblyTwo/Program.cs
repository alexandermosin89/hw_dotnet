using System;
namespace AssemblyTwo
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Nothing will work here, because we need add reference to project "AssemblyOne"!
            Motorcycle minsk = new Motorcycle();
            SportBike Jawa = new SportBike();
            minsk.fueltank = 40; no access
            minsk.fueltankPrivate = 40; no access
            minsk.fueltankProtectedPrivate = 15; no access
            minsk.odometr = 15; no access
            minsk.odometrProtected = 22222; no access
            minsk.odometrInternal = 500;
            minsk.odometrProtectedInternal = 1500;
            minsk.StartEngine(); no access
            minsk.StartEnginePrivate(); no access
            minsk.StopEngineProtPriv(); no access
            minsk.StopEngineProtected(); no access
            minsk.StopEngineInternal();
            minsk.StopEngineInternalProtected();
            minsk.StopEnginePublic();
            Jawa.MaxSpeedProtected = 500; no access
            Jawa.MaxSpeedProtectedInternal = 501;
            Jawa.MaxSpeedPrivateProtected = 502; no access
            */
        }
    }
}
